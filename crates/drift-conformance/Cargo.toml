[package]
name = "drift-conformance"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
drift-core = { path = "../drift-core" }
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
futures = "0.3"

[dev-dependencies]
axum = "0.6"
drift-coordinator-sqlite = { path = "../drift-coordinator-sqlite" }
drift-coordinator-postgres = { path = "../drift-coordinator-postgres" }
drift-coordinator-redis = { path = "../drift-coordinator-redis" }
drift-coordinator-memory = { path = "../drift-coordinator-memory" }
drift-coordinator-http = { path = "../drift-coordinator-http" }
drift-coordinator-server = { path = "../drift-coordinator-server" }
